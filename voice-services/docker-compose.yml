# Local: create `voice-services/.env` with SARVAM_TTS_API_KEY and SARVAM_STT_API_KEY (see main.py Settings).
services:
  voice-services:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-8080}:8080"
    env_file:
      - .env
    environment:
      HOST: "0.0.0.0"
      PORT: "8080"
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
